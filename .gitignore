gm.local.md
.claude/
.env
config/devnet.env
frontend/.env.local

# TypeScript build artifacts (tsc -b emits in-place without outDir)
tsconfig.tsbuildinfo
*.tsbuildinfo
scripts/*.js
tests/*.js
automation/src/*.js

# Anchor / Solana (target/* ignores contents, not the dir itself, so negations work)
target/*
!target/types/
!target/idl/
target/types/*.js
.anchor/
test-ledger/
.localnet/
.wallets/
node_modules/

# Vite build artifact (tsc emits .js next to .ts config)
frontend/vite.config.js
